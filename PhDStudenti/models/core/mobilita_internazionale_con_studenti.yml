version: 2

models:
  - name: mobilita_internazionale_con_studenti
    description: "International mobility activities. Matricola is filled from students via (cognome,nome) only when unique."
    columns:
      - name: id
        tests:
          - not_null
          - unique