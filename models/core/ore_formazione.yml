version: 2

models:
  - name: ore_formazione
    description: "Training hours summary per student (core)."
    columns:
      - name: id
        tests:
          - not_null
          - unique

      - name: matricola
        tests:
          - not_null
          - relationships:
              to: ref('students')
              field: matricola_dottorando